spring:
  application:
    name: langchain4j
server:
  port: 8080
hailiang:
  langchain4j:
    llm:
      # ???
      provider-name: openai
      # ???
      model-name: gpt-3.5-turbo
      # url
      base-url: https://api.chatanywhere.tech/v1
      # api-key
      api-key: ?????